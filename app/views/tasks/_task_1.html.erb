<turbo-frame id="task_detail">
  <div class="detail-content bg-white rounded-xl border border-gray-200 flex flex-col">
    <div class="px-6 py-4 border-b border-gray-100">
      <span class="text-xs font-medium text-gray-400 uppercase tracking-wide">Task 1 of 5</span>
    </div>
    <div class="px-6 py-6">
      <h2 class="text-lg font-bold text-gray-900 mb-1">Set up your first class</h2>
      <p class="text-sm text-gray-500 mb-6">Your classes are already imported. Just confirm the subject and level so the AI tutor knows what to focus on.</p>

      <% if completed && teacher.subject.present? %>
        <div class="bg-gray-50 rounded-xl border border-gray-200 p-4 mb-6">
          <p class="text-xs font-semibold text-gray-500 uppercase tracking-wide mb-3">Year 10 Set 2</p>
          <div class="grid grid-cols-2 gap-3">
            <div>
              <p class="text-xs font-medium text-gray-400 mb-1">Subject</p>
              <p class="text-sm font-semibold text-gray-800"><%= teacher.subject %></p>
            </div>
            <div>
              <p class="text-xs font-medium text-gray-400 mb-1">Curriculum level</p>
              <p class="text-sm font-semibold text-gray-800"><%= teacher.curriculum_level %></p>
            </div>
          </div>
        </div>
      <% else %>
        <%= form_with url: task_completions_path, method: :post, id: "setup-form" do |f| %>
          <%= hidden_field_tag :teacher_id, teacher.id %>
          <%= hidden_field_tag :onboarding_task_id, task.id %>

          <div class="bg-gray-50 rounded-xl border border-gray-200 p-4 mb-6">
            <p class="text-xs font-semibold text-gray-500 uppercase tracking-wide mb-3">Year 10 Set 2</p>
            <div class="grid grid-cols-2 gap-3">
              <div>
                <label class="block text-xs font-medium text-gray-600 mb-1">Subject</label>
                <select name="subject"
                        class="w-full text-sm border border-gray-200 rounded-lg px-3 py-2 bg-white focus:outline-none focus:ring-2 focus:ring-zg-blue">
                  <% %w[Mathematics English Science History Geography Languages Other].each do |s| %>
                    <option <%= "selected" if teacher.subject == s %>><%= s %></option>
                  <% end %>
                </select>
              </div>
              <div>
                <label class="block text-xs font-medium text-gray-600 mb-1">Curriculum level</label>
                <select name="curriculum_level"
                        class="w-full text-sm border border-gray-200 rounded-lg px-3 py-2 bg-white focus:outline-none focus:ring-2 focus:ring-zg-blue">
                  <% %w[GCSE KS3 A-Level].each do |l| %>
                    <option <%= "selected" if teacher.curriculum_level == l %>><%= l %></option>
                  <% end %>
                </select>
              </div>
            </div>
          </div>

          <button type="submit"
                  class="w-full bg-zg-blue text-white text-sm font-semibold py-3 px-4 rounded-xl hover:bg-blue-700 transition-colors cursor-pointer">
            Confirm setup →
          </button>
        <% end %>
      <% end %>

      <%= render "tasks/task_footer", completed: completed, completion_id: completion_id %>
    </div>
  </div>
</turbo-frame>
